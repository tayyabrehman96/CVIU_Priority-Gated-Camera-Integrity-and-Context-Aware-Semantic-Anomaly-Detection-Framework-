# Research / benchmark configuration (optional overrides)
# Copy to `.env` if you need non-default paths or tuning.

VIDEO_FOLDER=videos
FRAME_SKIP=2
PROCESS_FPS=10.0

# Optional: custom weapon-detector weights (default: models/weapon/weights/best.pt)
# WEAPON_MODEL_PATH=models/weapon/weights/best.pt
